# T00 — repository and build foundation

Status: implemented and independently verified locally; remote CI/integration pending at user-requested usage wrap-up. Dependencies: none. Branch: codex/t00-build-scaffold. Evidence: docs/verification/T00.md.

Deliverable: pinned workspace, minimal browser shell, native Rust runner, real worker-to-Wasm smoke path, CI and an executed clean-checkout procedure. No scheduling behavior belongs in this task.

Implementation owner: t00_scaffold (tier B responsibility, inherited model settings). Owns apps/, crates/, scripts/, tests/, root manifests and lockfiles, toolchain files, .github/ and .gitignore. Orchestrator owns AGENTS.md, CONTRIBUTING.md, README.md, IMPLEMENTATION-TODO.md and docs/. The orchestrator independently reviews the scaffold and its evidence before accepting T00.

Read specification sections 5 (architecture), 10 (robustness), 11 (verification) and 13 (acceptance context). Resolve dependency/toolchain versions against official release documentation, record exact source pages and selected versions. This task's smoke function is infrastructure only and establishes no simulation model contract.

Acceptance: native and browser/Wasm production builds pass; a real browser worker invokes the compiled Rust function with varied inputs; missing/broken Wasm causes a visible failure rather than success; adapters contain no simulation policy. Tests have independent expected results and documented negative controls. Document commands after script names exist and before accepting execution evidence.

Escalate undefined simulation semantics to T01/T02; do not implement them here. Treat toolchain/network failures as explicit setup blockers with exact error evidence. Follow sandbox escalation requirements. Report any unavailable browser verification as not run.

Review includes checking tests cannot pass from a constant ready message or mock bridge. Review toolchain pinning, generated output exclusions, lockfiles, failure propagation and clean-checkout dependencies. No task or milestone is completed solely because CI configuration exists.

Settled verification commands: `npm ci --include=optional`, `npx playwright install chromium`, then `npm run check`. The check aggregates Prettier, TypeScript, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo test --workspace --locked`, `cargo build --workspace --release --locked`, Wasm/browser production builds and Playwright. Linux CI installs browser OS dependencies with `npx playwright install --with-deps chromium`. Native Windows builds require MSVC build tools. Rust and its Wasm target must be installed and cargo available on PATH.

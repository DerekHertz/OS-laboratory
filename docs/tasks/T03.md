# T03 — event kernel and M1 interpreter

Baseline: merged T02 `e99d65a`. Branch: `codex/t03-event-kernel`. Integration owner/reviewer coordinator: root orchestrator. Scope for this session: T03; T04/T05 remain separate policy tasks.

## Author packet

- Dependencies: T00, T01, T02 integrated.
- Tier A: GPT-6 Astra, high reasoning. Agent `t03_kernel`.
- Owned paths: `crates/sim-core/**`, `Cargo.lock` (only necessary pinned Rust decoding dependencies), `docs/verification/T03.md`, `docs/decisions/t03-kernel.md`. No adapter/UI changes. Ask orchestrator before any other owned-path change.
- Contracts: spec §§5–8, 10–11; all state/order/control/error rules of `content/concepts/scheduling-m1.md`; program, workload, integer, event and source mapping rules of `content/concepts/program-transport-m1.md`; shared schema and TypeScript projection; existing independent scheduling/reference contracts fixtures.
- Deliver checked time and insertion counters, deterministic event ordering and generation invalidation, lifecycle ownership, immutable compute/ioWait/yield/repeat/end interpretation, run-wide control budget and fatal cleanup, source identity, and Rust workload decoding/validation evidence against T02. Keep build_probe compatibility. Supply a narrow policy interface for later T04/T05; any test policy must be explicitly test-only and cannot substitute for claimed policy integration.
- Acceptance: independently derived lifecycle/interpreter and failure cases, repeated deterministic canonical output, large integer lossless decoding and invalid input rejection, stable phase closure, no UI/browser dependencies. Explicitly distinguish deferred policy/metrics/retention/worker/replay work.
- Checks: cargo fmt --all -- --check; cargo clippy --workspace --all-targets --locked -- -D warnings; cargo test --workspace --locked. Use C:\Users\Derek\.cargo\bin in PATH. Record every test family using falsifiable evidence and meaningful negative controls where practical. Root owns full application check.
- Reviewer: fresh independent tier-A agent after author checkpoint. A separate read-only acceptance auditor will inspect boundary/cases in parallel.
- Escalate: missing semantic rule, public-contract change, inconsistent reference outcome, decoding incompatibility, inability to finish bounded T03 with safe context. Document contract decisions for approval by orchestrator before depending on changed semantics. Checkpoint incomplete instead of claiming completion. Do not push or merge.

## Independent acceptance audit packet

- Agent `t03_acceptance`, GPT-5.6 Sol / xhigh, fresh context.
- Read-only repository ownership; report to root, no source edits. Dependencies and references as above.
- Deliver independent checklist/hand-derived edge cases for event order, repeat/control budget, stale tokens, fatal boundary, and T02 Rust decoding parity; identify ambiguities before code relies on them.
- Acceptance: concrete expected inputs/outcomes grounded in contracts. Review author design when available. Reviewer/integration owner: root.
- Escalate undefined semantics or scope that accidentally completes T04/T05 without separate review.

## Orchestrator prerequisite decisions

Repeat charging was independently resolved and committed at `1f1adb1`: one unit before each iteration body, no extra final-exit charge. Generated-ID correction was independently resolved and committed at `323af26`; see `docs/decisions/t03-contract-corrections.md` and its verification record. These shared prerequisites precede dependent kernel output.

Canonical-output refinements accepted after independent audit: per-core generation starts0 and increases once on reservation; ordered entity IDs are thread then core (thread-only for other thread events, empty for globals); thread event parents name the immediately previous event for that thread, while global terminal events name the immediately prior canonical event when present. This records predecessor provenance, not a complete dependency graph. Ordinary canonical event emission reserves sequence u64::MAX for a sole fatal error; exhaustion must be detected before the affected operation mutates state. Thread-attributed exhaustion is arithmeticOverflow; un-attributable global counter exhaustion is internalEngine under the existing typed error contract. T07 owns transport inspection sequences; T03 supplies typed stable kernel state, not a full worker snapshot/reply implementation. The author decision document and tests must make these boundaries concrete.

T02 integration was independently read from GitHub PR4: merged2026-09-11T14:45:45Z as e99d65adb17bceadf21dac2c4f92063b452d07bd, with three successful build-and-test check records. On2026-09-11 main protection was read back: strict build-and-test, PR requirement, admin enforcement, linear history, conversation resolution, no force-push/deletion. This verifies the baseline only; it does not claim T03 remote CI has run.

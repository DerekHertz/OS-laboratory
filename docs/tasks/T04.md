# T04 — production FCFS policy

Date: 2026-09-13. Branch `codex/t04-fcfs`; baseline/integrated dependency T03 `1e1681d`. Root owns readiness, review integration, remote integration and handoff.

## Author packet

- Tier B / medium: GPT-5.6 Luna / medium, agent `t04_fcfs`.
- Owned paths: `crates/sim-core/src/fcfs.rs`, `crates/sim-core/src/fcfs_tests.rs`, the minimal module export in `crates/sim-core/src/lib.rs`, `docs/decisions/t04-fcfs.md`, and `docs/verification/T04.md`. Ask root before touching any other path.
- Dependencies/contracts: T03 integrated; specification sections 6, 9 and 11; `content/concepts/scheduling-m1.md` FCFS, state/queue, dispatch cost and interval rules; independently worked cases C01, C03, C05, C06, C07, C08 and C10 as applicable. The T03 kernel remains authoritative for ordering and lifecycle.
- Deliver a production FCFS implementation of `kernel::Policy`: persisted `PolicyConfig::Fcfs`, no quantum, select only the global FIFO head without mutating the queue. Exercise it through the real T03 kernel, not a copied scheduler. No metrics aggregation or changes to kernel ordering.
- Acceptance: production policy reproduces applicable reference timelines/status/events and raw interval facts; nonzero dispatch overhead remains dispatching/core overhead while ready waiting ends on reservation; yield returns to the tail; immediate end reaches the phase-4 fixpoint; multicore selection is stable; blocked work is not selected because only ready indices reach the policy. Reject mismatched RR workload through the existing kernel constructor. Repeated runs match canonical events/state/intervals.
- Verification: `cargo fmt --all -- --check`; `cargo clippy -p sim-core --all-targets --locked -- -D warnings`; `cargo test -p sim-core fcfs --locked`; `git diff --check`. Record falsifiable test families and one meaningful policy-selection mutation or equivalent negative control, restore and rerun. No browser/full build.
- Reviewer: fresh tier-A reviewer after author commit. Escalate if the policy seam cannot express FCFS without kernel/contract changes, a reference case conflicts, or accounting facts cannot be distinguished. Checkpoint promptly and do not guess semantics. No push/merge/subagents.

## Reviewer packet

- Tier A: GPT-5.6 Sol / xhigh, fresh context; owns only `docs/reviews/T04-REVIEW.md`.
- Independently compare the implementation with FCFS contract and worked cases. Run focused tests and at most one targeted probe for a concrete concern. Report ACCEPT or CHANGES REQUIRED; no source changes or broader task work.

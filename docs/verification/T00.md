# T00 verification record

2026-09-09. Source commit c0fb662, branch codex/t00-build-scaffold. Implementation: t00_scaffold; independent review and clean-checkout/mutation execution: orchestrator. Windows, Node 22.20.0, npm 10.9.3, Rust 1.94.1, MSVC, Chromium 153.0.8010.12 (Playwright 1.63.0).

Local checks passed. Remote CI/integration remains pending at user-requested usage wrap-up. M0 is not complete.

| Test/check | Null hypothesis | Testing for / expected result | Actual | Issue response |
|---|---|---|---|---|
| Rust independently_worked_probe_cases | Arithmetic is wrong or constant. | Five independent modulo-2^32 cases: 0+0=0, 17+25=42, 123+456=579, max+1=0, max+max=4294967294. | All passed. | Correct core and rerun; preserve independent expectations. |
| Native real_native_process_accepts_values_and_rejects_invalid_input | CLI returns wrong output or accepts invalid arguments. | Real process: 17+25 prints 42; max+1 prints 0; negative, overflow and missing arguments exit 2 with usage. | All five cases passed. | Correct parsing/forwarding/core and rerun actual executable. |
| Browser real worker executes compiled Rust | Bridge returns incorrect or hardcoded values. | Startup 42, four fixed boundaries and 12 runtime-random pairs equal independent BigInt modulo oracle. Failures identify operands. | Passed in original, clean and restored runs. | Trace actual worker/Wasm/core; replay operands without mocking bridge. |
| Browser absent Wasm control | UI falsely succeeds without Wasm. | Abort external .wasm request; require interception, visible failure and no result; unblock/reload and expect 42. | Passed, including recovery. | Correct failure propagation; retain interception assertion. |
| Browser out-of-range input | UI accepts values above u32. | Enter 4294967296 and expect validation message. | Passed. Establishes visible rejection for this case. | Correct validation and rerun. |
| Prettier and cargo fmt checks | Source violates formatting. | Both exit 0. | Passed. | Format affected files and rerun. |
| TypeScript typecheck | Included application source has type errors. | tsc exits 0. | Passed. | Correct types rather than suppress findings. |
| Clippy workspace/all-targets, warnings denied | Rust triggers configured lint errors. | Exit 0. | Passed. | Resolve finding and rerun. |
| Native release, Wasm and browser builds | A build path or bundled asset is broken. | All exit 0; external worker/Wasm emitted. | Passed; Wasm 0.41 kB, worker 0.55 kB. | Correct failing stage and verify runtime separately. |
| Clean checkout | Success depends on untracked build artifacts. | Clone committed source, install lockfile and run full check without copying generated output. | Passed; 3 browser tests in 5.2 seconds. | Track missing inputs/document prerequisites, then repeat fresh. |
| Constant-return mutation | Tests pass despite hardcoded 42. | Replace core result with 42 in isolated clone; core and rebuilt browser positive tests must fail. | Both failed: 42 versus 0 for inputs 0,0. Both commands exited nonzero. | Restored source; full check passed again, including 3 browser tests in 4.8 seconds. |

Clean-clone commands (cargo directory prepended to PATH):

```text
git clone --local --branch codex/t00-build-scaffold D:/codex/_projects/OS-laboratory .verification/t00-clean
npm ci --include=optional --os=win32
npm run check
# Deliberately replace isolated core result with 42.
cargo test -p sim-core --locked
npm run build
npx playwright test --grep "real worker executes"
# Restore original source.
npm run check
```

The full check includes Rust unit/CLI tests, formatting, typecheck, Clippy, native release, Wasm/browser builds and browser tests. The clean clone reused installed machine prerequisites and the agent-installed Playwright browser; it was not a pristine OS. The agent separately installed Chromium and passed the original full check.

Issue found: existing npm os setting was linux while Node reported win32, causing missing Windows Rolldown bindings. Including optional packages alone did not fix it. Command-specific --os=win32 fixed selection without changing global config. Git ownership checks required scoped safe.directory/escalation.

Independent review addressed inlined Wasm, vacuous request interception, constant fixture results, ambient Cargo target directory and mutable CI action tags. Source boundaries and shell screenshot inspected. This provides no simulation correctness, performance, full accessibility or M1 evidence. Remote CI and protection are unverified.

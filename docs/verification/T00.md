# T00 verification record

Status: in progress, not acceptance evidence yet. Date: 2026-09-09. Branch: codex/t00-build-scaffold, based on 6e129aa. Windows host; Node 22.20.0, npm 10.9.3. Toolchain installation and build verification are in progress.

The following expectations are recorded before the build results. Replace pending actuals only with observed execution evidence. Individual test names and exact commands must be added when available.

| Check | Null hypothesis | Testing for / independent expectation | Actual | Issue response |
|---|---|---|---|---|
| Native build and smoke | The Rust workspace cannot build or its smoke function ignores supplied input. | Compile and run the native runner with varied inputs; compare arithmetic with independently computed results. | Pending. | Correct toolchain/build or input forwarding, then rerun the failing cases. |
| Browser production build | The frontend cannot compile or bundle the Wasm worker assets. | Type checking and a production build exit successfully and emit loadable worker/Wasm assets. | Pending. | Correct bundling/type errors and rebuild; compilation alone does not establish runtime success. |
| Real worker/Wasm execution | The UI can report success without the worker executing the compiled Rust function. | Real browser executes varied inputs across worker/Wasm boundary and returns independently expected results. | Pending. | Trace input transport, Wasm loading and UI response; do not replace the worker with a mock. |
| Missing Wasm negative control | The smoke check still succeeds when its Wasm dependency cannot load. | Deliberately block Wasm loading; require visible failure and no ready/success result. Restore loading and rerun the positive check. | Pending. | Remove false success paths, improve failure reporting and rerun both controls. |
| Clean checkout | Successful builds depend on untracked local artifacts. | Execute the documented install/build/test procedure in a fresh tracked-source checkout. | Pending. | Add missing tracked inputs or documented prerequisites, then repeat from a fresh directory. |

Repository inspection: GitHub reports no classic branch protection and no rulesets; current account has repository ADMIN permission. Remote CI and protection are not yet verified. No simulation correctness, performance, accessibility or M1 claim follows from T00.

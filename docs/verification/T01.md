# T01 verification record

Status: author checks passing; independent tier-A semantic review pending. Date: 2026-09-09. Branch: `codex/t01-scheduling-contract`.

The fixtures were worked from `sched.m1` before any production event kernel, FCFS policy, or RR policy exists. Automated checks below validate encoding and arithmetic conservation only; they do not independently establish that the semantic choices are correct.

| Test/check                         | Null hypothesis                                                                                             | Testing for / expected result                                                                                                                             | Actual                                                                                                                  | Issue response                                                                                                 |
| ---------------------------------- | ----------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| Fixture JSON parse                 | The reference fixture is not machine-readable JSON.                                                         | Node parses `cases.json`; exactly seven named cases are available.                                                                                        | Passed; seven cases parsed.                                                                                             | Correct the test-only encoding without changing semantics, then rerun.                                         |
| Core interval ledger               | A core ledger has a gap/overlap or totals inconsistent with its intervals/window.                           | Every core begins at 0, has contiguous nonnegative half-open intervals ending at `finalTick`, and recomputed useful/overhead/idle equals recorded totals. | Passed for all seven cases.                                                                                             | Rework the hand calculation and reconcile Markdown/JSON; do not derive expectations from future engine output. |
| Thread conservation                | A completed thread's accounting components do not equal turnaround.                                         | `readyWait + dispatchOverhead + blocked + useful = turnaround`; response does not exceed turnaround.                                                      | Passed for every thread in all seven cases.                                                                             | Recalculate state intervals and correct the erroneous expectation with review.                                 |
| Useful-service conservation        | Thread and core ledgers disagree about useful work.                                                         | Sum of per-core useful ticks equals sum of per-thread useful ticks in each case.                                                                          | Passed for all seven cases.                                                                                             | Locate the missing/double-counted interval and correct both narrative and fixture.                             |
| Prettier                           | Fixture formatting is unstable.                                                                             | `npm run format:check` exits 0.                                                                                                                           | Passed.                                                                                                                 | Format affected files and rerun.                                                                               |
| Patch whitespace                   | Changes contain whitespace errors.                                                                          | `git diff --check` exits 0.                                                                                                                               | Passed.                                                                                                                 | Correct reported lines and rerun.                                                                              |
| Existing workspace regression gate | Contract/fixture additions break the established native, Wasm, browser, type, lint, or formatting baseline. | With the pinned Cargo directory on `PATH`, `npm run check` completes all stages and all three browser tests pass.                                         | Passed: format, types, Clippy, Rust tests/docs, native release, Wasm/web production builds, and three Playwright tests. | Diagnose the failing stage without changing semantic expectations merely to restore green.                     |
| Independent semantic review        | Author reasoning contains an ambiguous or incorrect scheduling rule/case.                                   | A separate tier-A reviewer recomputes cases, challenges phase/state rules, and records approval or findings before T03.                                   | Pending; no independent reviewer assigned in this session.                                                              | Assign a separate reviewer; resolve findings without weakening cases.                                          |

Executed arithmetic check: Node parsed the fixture, recomputed contiguous interval totals, per-thread turnaround conservation, response bounds, and cross-ledger useful totals; it printed `7 cases: JSON, interval, thread, and core conservation passed`. The first aggregate-check attempt stopped at Cargo because this shell lacked Cargo on `PATH`; rerunning with `C:\Users\Derek\.cargo\bin` prepended passed the complete established gate. This is bounded author evidence, not a proof of scheduling correctness.

Coverage includes simultaneous arrivals, compute/quantum equality, switch cost including idle dispatch, same-tick I/O wakeup/expiration, delayed arrival with idle cores, stable two-core dispatch/completion, and explicit yield. It does not cover overflow/error/quiescence interpreter behavior, which T03 must fixture after T02 settles encoding.

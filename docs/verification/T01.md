# T01 verification record

Status: independent semantic review accepted; final-head protected remote CI and integration pending. Date: 2026-09-09. Branch: `codex/t01-scheduling-contract`.

The fixtures were worked from `sched.m1` before any production event kernel, FCFS policy, or RR policy exists. Automated checks below validate encoding and arithmetic conservation only; they do not independently establish that the semantic choices are correct.

| Test/check                         | Null hypothesis                                                                                                  | Testing for / expected result                                                                                                                                                                                  | Actual                                                                                                                  | Issue response                                                                                                 |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| Fixture JSON parse                 | The reference fixture is not machine-readable JSON.                                                              | Node parses `cases.json`; exactly ten unique named cases are available.                                                                                                                                        | Passed; ten unique cases parsed.                                                                                        | Correct the test-only encoding without changing semantics, then rerun.                                         |
| Core interval ledger               | A core ledger has a gap/overlap or totals inconsistent with its intervals/window.                                | Every nonempty core ledger begins at 0, has contiguous nonnegative half-open intervals ending at `finalTick`, and recomputed useful/overhead/idle equals recorded totals; the zero-tick error ledger is empty. | Passed for all ten cases.                                                                                               | Rework the hand calculation and reconcile Markdown/JSON; do not derive expectations from future engine output. |
| Thread conservation                | A completed thread's accounting components do not equal turnaround, or unfinished work is reported as completed. | For completed threads, `readyWait + dispatchOverhead + blocked + useful = turnaround`; numeric response does not exceed turnaround. Error/censored threads have `null` turnaround.                             | Passed for every thread in all ten cases.                                                                               | Recalculate state intervals/status and correct the erroneous expectation with review.                          |
| Useful-service conservation        | Thread and core ledgers disagree about useful work.                                                              | Sum of per-core useful ticks equals sum of per-thread useful ticks in each case.                                                                                                                               | Passed for all ten cases.                                                                                               | Locate the missing/double-counted interval and correct both narrative and fixture.                             |
| Prettier                           | Fixture formatting is unstable.                                                                                  | `npm run format:check` exits 0.                                                                                                                                                                                | Passed.                                                                                                                 | Format affected files and rerun.                                                                               |
| Patch whitespace                   | Changes contain whitespace errors.                                                                               | `git diff --check` exits 0.                                                                                                                                                                                    | Passed.                                                                                                                 | Correct reported lines and rerun.                                                                              |
| Existing workspace regression gate | Contract/fixture additions break the established native, Wasm, browser, type, lint, or formatting baseline.      | With the pinned Cargo directory on `PATH`, `npm run check` completes all stages and all three browser tests pass.                                                                                              | Passed: format, types, Clippy, Rust tests/docs, native release, Wasm/web production builds, and three Playwright tests. | Diagnose the failing stage without changing semantic expectations merely to restore green.                     |
| Remote CI on author commit         | Local success depends on the Windows workspace or untracked artifacts.                                           | Both GitHub `build-and-test` runs on commit `aed2efa` complete successfully in the declared Linux workflow.                                                                                                    | Passed in 55 seconds and 57 seconds on draft PR #2.                                                                     | Inspect the failing remote stage and reconcile environment or tracked-source differences.                      |
| Independent semantic review        | Author reasoning contains an ambiguous or incorrect scheduling rule/case.                                        | A separate tier-A reviewer recomputes cases, challenges phase/state rules, and records approval or findings before T03.                                                                                        | Passed on focused re-review: F01–F03 closed and C08–C10 independently reproduced; verdict `ACCEPT`.                     | Reopen T01 if implementation later exposes a contradiction with the accepted contract.                       |

Executed arithmetic check: Node parsed the fixture, checked ten unique IDs, recomputed contiguous interval totals, per-thread completion/censoring rules, response bounds, and cross-ledger useful totals; it printed `10 cases: JSON, interval, status, thread, and core conservation passed`. An in-memory negative control changed C08's `finalTick` from 1 to 2; the same interval-end invariant rejected it with `C08-zero-cost-immediate-end-fixpoint final`, confirming that the checker does not accept an inconsistent ledger unconditionally. After the fixer edits, `$env:PATH = 'C:\Users\Derek\.cargo\bin;' + $env:PATH; npm run check` passed formatting, types, Clippy, Rust tests/docs, native release, Wasm/web production builds, and all three Playwright tests. This is bounded author evidence, not a proof of scheduling correctness.

Orchestrator final-candidate verification after review acceptance and the editorial clarification reran the same full command. Formatting, TypeScript, Rustfmt, Clippy, Rust tests/docs, native release, and Wasm/web production builds passed. Its first browser invocation was blocked by a stale OS Laboratory preview on port 4173. After verifying the listener served this workspace's exact build and stopping only that listener, two browser reruns each passed all three assertions. In this sandbox the Playwright process then remained alive during preview-server teardown and was interrupted after 40 seconds with no listener left. Protected Linux CI on the committed final head remains the authoritative clean-process gate; the teardown interruption is not recorded as a completed local `npm run check` pass.

## Corrective fixture evidence

| Case                        | Null hypothesis                                                                                                                      | What is exercised                                                                  | Independently expected result                                                                                                                                                                                                                 | Actual result                                                                    | Failure handling                                                                                               |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| C08 phase-4 fixpoint        | A zero-cost dispatch that immediately ends can strand B ready on an idle core or require an invented later tick.                     | One core; A `end`; B `compute 1; end`; both arrive at 0.                           | Scan 1 ends A at 0; scan 2 starts B at 0; B uses `[0,1)`; final tick 1. A is completed with response `null`, turnaround 0; B is `(0,1,0,0,0,1)`. Core totals are `1/0/0`.                                                                     | README and JSON encode exactly this result; arithmetic/status validation passed. | Re-derive scan ordering and correct both contract and fixture; never add an idle tick without a modeled event. |
| C09 run-fatal control error | Exhausting the per-tick control budget can allow later dispatch, leave ownership/tokens live, or count unfinished work as completed. | Budget 3; A needs four same-tick repeat evaluations; B is ready with compute work. | Operations 1–3 execute; operation 4 does not. Fatal boundary is tick 0; A=`error`, B=`censored`; both response/turnaround `null`; empty `[0,0)` core ledger, zero completed, throughput/utilization `null`, and all pending work invalidated. | README and JSON encode exactly this result; arithmetic/status validation passed. | Treat any differing continuation or accounting as a contract/engine defect; do not weaken the error assertion. |
| C10 completed no-compute    | A completed program without compute is forced to invent a numeric response or cannot be represented.                                 | A executes `io_wait 1; end` with zero switch cost.                                 | A blocks `[0,1)`, ends at 1, and is completed with `(null,1,0,0,1,0)`; core idle is `[0,1)` and throughput is 1.                                                                                                                              | README and JSON encode exactly this result; arithmetic/status validation passed. | Preserve the valid program and fix consumers/schema to retain `null` response distinctly from zero.            |

Coverage now includes simultaneous arrivals, compute/quantum equality, switch cost including idle dispatch, same-tick I/O wakeup/expiration, delayed arrival, stable two-core dispatch/completion, explicit yield, zero-time phase-4 redispatch, run-fatal budget error, and a completed no-compute program. Runtime overflow remains in the same run-fatal family but has no separate T01 fixture; T03 must exercise it after T02 settles encoding.

# T02 verification record

Status: author checks passing; independent tier-A contract review pending. Date: 2026-09-09. Branch: `codex/t02-program-transport-contract`.

| Test/check | Null hypothesis | What is exercised / expected | Actual | Failure response |
| --- | --- | --- | --- | --- |
| Shared-schema compilation | The canonical schema is invalid or a union is ambiguous. | Ajv 8.20.0 compiles draft 2020-12 in strict mode; every valid case matches once and every invalid case is rejected. | Passed for 21 cases. | Correct the schema/fixture contract; do not loosen closed unions merely to pass. |
| Exact integers | JSON transport loses `u64` precision or accepts noncanonical forms. | Maximum `u64` remains a string; JSON number and `u64 + 1` inputs fail. | Passed; overflow negative control rejected. | Preserve decimal strings and checked Rust parsing. |
| Program semantic limits | Malformed control flow or resource-heavy imports pass validation. | Duplicate IDs, missing/reachable `end`, invalid ranges/references, depth 65, 10,001 blocks, and >1 MiB input fail. | Passed, including generated size/depth/count controls. | Fix the semantic validator and add the smallest reproducer. |
| Version separation | Schema/protocol/engine compatibility can be silently coerced. | Unknown program schema, protocol, and engine versions fail while application/model/random/seed fields remain separate. | Passed. | Add an explicit migration/unsupported-version path; never replay silently. |
| Snapshot and delta integrity | A client can apply a sequence gap or observe contradictory queue/core ownership. | Gap application returns false; ready/core/thread cross-references and event sequences/payloads are checked. | Passed; gap and broken-ownership controls rejected. | Require full resynchronization and correct the producer. |
| TypeScript projection | The consumer projection contains a type error or omits the contract source tree. | Strict `tsc --noEmit` includes `packages/contracts/src`. | Passed. | Reconcile projection and schema before implementation. |
| Formatting/whitespace | Source formatting is unstable or patch whitespace is invalid. | Prettier and `git diff --check` exit zero. | Passed. | Format only affected files and rerun. |

The size, depth, block-count, overflow, unsupported-engine, and sequence-gap controls deliberately corrupt otherwise valid structures and confirm rejection. These finite cases are bounded evidence. Rust/native/Wasm decoding equivalence is not yet claimed and is required in T03.

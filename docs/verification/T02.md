# T02 verification record

Status: review corrections pass author checks; independent tier-A re-review pending. Date: 2026-09-10. Branch: `codex/t02-program-transport-contract`.

| Test/check | Null hypothesis | What is exercised / expected | Actual | Failure response |
| --- | --- | --- | --- | --- |
| Shared-schema compilation | The canonical schema is invalid or a union is ambiguous. | Ajv 8.20.0 compiles draft 2020-12 in strict mode; every valid case matches once and every invalid case is rejected. | Passed for 28 cases. | Correct the schema/fixture contract; do not loosen closed unions merely to pass. |
| Exact integers | JSON transport loses `u64` precision or accepts noncanonical forms. | Maximum `u64` remains a string; JSON number and `u64 + 1` inputs fail. | Passed; overflow negative control rejected. | Preserve decimal strings and checked Rust parsing. |
| Program semantic limits | Malformed control flow or resource-heavy imports pass validation or crash it. | Duplicate IDs, missing/reachable `end`, invalid ranges/references, an unknown operand parameter, depth 65, 10,001 blocks, and >1 MiB input fail cleanly. | Passed, including the former missing-declaration crash and generated size/depth/count controls. | Fix the semantic validator and add the smallest reproducer. |
| Version and error separation | Schema/protocol/engine/random compatibility can be silently coerced or assigned inconsistent terminal metadata. | Unknown program schema, protocol, engine, and random-algorithm versions fail distinctly; pre-ledger errors remain sequence zero and nonterminal. | Passed. | Add an explicit migration/unsupported-version path; never replay silently. |
| Event identity and mapping | A source-bearing event can omit the thread or block needed for authoritative inspection. | Kind-specific required/forbidden thread and block identity is checked; a source-less compute event fails. | Passed. | Reconcile prose, schema, projection, and producer validation. |
| Snapshot and delta integrity | A client can apply a sequence gap or observe contradictory queue/core ownership. | Gap application returns false; full resync is explicit; ghost queue IDs, stable ready+idle states, event continuity, and invalid base-plus-delta final ownership fail. | Passed; valid and invalid base-plus-delta cases exercised. | Reject the whole delta and request `{ mode: "full" }`. |
| Checkpoint binding | A checkpoint ID can be reused or detached from replay compatibility and state position. | Reply ID is run-namespaced and binds exact model/engine/random versions, tick, and matching state sequence. | Passed for the reply boundary; cross-reply non-reuse remains an engine-ledger invariant for T13. | Reject mismatched/reused bindings as a checkpoint or compatibility error. |
| TypeScript projection | The consumer projection contains a type error or omits the contract source tree. | Strict `tsc --noEmit` includes `packages/contracts/src`. | Passed. | Reconcile projection and schema before implementation. |
| Formatting/whitespace | Source formatting is unstable or patch whitespace is invalid. | Prettier and `git diff --check` exit zero. | Passed. | Format only affected files and rerun. |
| Repository-wide gate | T02 breaks an existing native, Wasm, web, or browser integration. | With `C:\Users\Derek\.cargo\bin` prepended to `PATH`, `npm run check` must pass formatting, 28 contract cases, TypeScript, Rustfmt, Clippy, Rust tests/docs, native release, Wasm/web builds, and three browser tests. | Passed on 2026-09-10. All three browser tests passed; the Playwright preview processes again remained alive after results and were stopped by exact PID, after which the command exited 0 with `3 passed (1.8m)`. | Treat a test failure as open; diagnose the separately known preview-teardown delay without weakening browser assertions. |

The size, depth, block-count, overflow, unsupported-engine/random-algorithm, and sequence-gap controls deliberately corrupt otherwise valid structures and confirm rejection. The routing precedence and checkpoint non-reuse rules are normative multi-message invariants; T07/T13 must add stateful integration tests. These finite cases are bounded evidence. Rust/native/Wasm decoding equivalence is not yet claimed and is required in T03.

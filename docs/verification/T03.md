# T03 author verification

Candidate evidence, 2026-09-11. Worktree: `D:/codex/_projects/OS-laboratory/.verification/worktrees/t03`; branch `codex/t03-event-kernel`. This author record does not replace independent semantic acceptance, protected CI, production policy integration, or native/Wasm simulation equivalence.

Every family below uses actual decoder/kernel execution. The shared helper checks ownership and absence of ready-plus-idle at every returned boundary. The test-only FIFO/quantum driver is not a shipped T04/T05 policy. Expected reference intervals were authored in T01 before this implementation; explicit edge values below are independently worked from contracts.

| Test or family                                                  | Null hypothesis (failure claim)                                                         | Exercise and independently expected result                                                                                                                                                                                                                                | Actual result / failure handling                                                                                                                                                                     |
| --------------------------------------------------------------- | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `independently_worked_probe_cases`                              | Existing adapter probe behavior regresses.                                              | Five T00 arithmetic cases, including modular overflow; unchanged expected values.                                                                                                                                                                                         | Passing; T00 remains the source of this existing evidence.                                                                                                                                           |
| `reference_lifecycle_intervals_with_test_only_policy`           | Kernel lifecycle or phase order disagrees with T01.                                     | All C01–C10, checking exact final ticks, every core interval, terminal/error state, and a repeated run's complete events/state/intervals.                                                                                                                                 | Passing all 10 cases twice; this verifies kernel scheduling mechanisms using a test driver. Fix semantic mismatch rather than altering T01 expectations.                                             |
| `repeat_charge_boundaries_and_source_identity`                  | Repeat bookkeeping charges the wrong operations or loses original sources.              | Empty repeat3/end succeeds with budget4; budget3 errors at end; repeat4/budget3 errors on loop. Repeat3/compute1 with budget1 finishes at3; nested 2x2 at4; end inside repeat terminates immediately.                                                                     | Passing. Reconcile any failure with C09 and documented iteration charging.                                                                                                                           |
| `fatal_budget_is_run_wide_and_stops_later_phases`               | Threads or redispatch reset the budget, or fatal processing executes later work.        | Two ends with budget1 preserve first terminated and error second. Compute1/repeat2 budget1 errors phase1 at1 before second thread arrival. Yield/repeat cycle uses exactly5 control units, then fails on yield.                                                           | Passing; pending heap empty, future thread censored, repeat terminal advance emits nothing.                                                                                                          |
| `phase_order_quantum_instruction_match_and_stale_tokens`        | Quantum or instruction boundaries run in the wrong order.                               | Compute5/grant2 expires twice and completes once under generation3 at5. At2, compute boundary/start precede arrival then expiration, which schedules the arriving thread. Compute1/I/O1 under grant2 wakes at2 and finishes3 without expiration.                          | Passing; expected event order is explicitly asserted.                                                                                                                                                |
| `phase_four_scans_finish_ascending_cores`                       | Immediate end restarts a scan too early or later completions are re-sorted.             | A=end, B/C=compute1 on2 cores: starts A/core0, B/core1, C/core0; completions retain insertion order core1 then core0.                                                                                                                                                     | Passing. Repair scan/heap order on failure.                                                                                                                                                          |
| `arithmetic_failures_close_active_and_future_ownership`         | Overflow wraps or fatal cleanup loses ongoing accounting.                               | Arrival MAX plus switch1, compute1, or I/O1 fails atMAX; core idle interval totals MAX. At1 fatal with another running and a future thread closes both cores at1 and censors both other threads.                                                                          | Passing; no fabricated termination.                                                                                                                                                                  |
| `checked_counter_overflow_and_reserved_terminal_event`          | Internal counters wrap or fatal event cannot be represented.                            | Seed insertion/core/instruction counters atMAX; each reports attributable overflow. Seed event counter MAX-1: runtimeError alone atMAX, no arrival/dispatch. Exhaust ordinary events at runCompleted: preserve terminated thread and emit global internalEngine atMAX.    | Passing. Counter injection is test-only and does not claim a feasible natural MAX-event workload.                                                                                                    |
| `insertion_preflight_rejects_before_operation_event`            | Scheduling exhaustion emits the failing operation before rejecting it.                  | Exhaust available slots for switch, compute, tied compute/expiration, and I/O. Expect error with no corresponding dispatchStarted/computeStarted/ioSubmitted, unchanged insertion count.                                                                                  | Passing. A deliberate bypass negative control is recorded below.                                                                                                                                     |
| `earliest_deadline_near_u64_limit`                              | Irrelevant deadlines cause premature overflow or equality skips phase3.                 | MAX-5/grantMAX/compute2 finishesMAX-3. MAX-5/grant2/compute10 expires twice then failsMAX-1. MAX-2/grant2/compute2 endsMAX. Equality followed by compute atMAX still expires once before the next positive deadline fails.                                                | Passing. Derivation uses checked addition only for the earliest required service boundary.                                                                                                           |
| `stale_tokens_do_not_advance_time_or_mutate_current_occurrence` | Old dispatch tokens change time/state.                                                  | After generation2 starts, inject generation1 compute, expiration and switch tokens at3 plus an obsolete far-future token; compare remaining real output/state/intervals with a clean run.                                                                                 | Passing; exact equality. Injected tokens challenge real applicability checks.                                                                                                                        |
| `generated_ids_causal_predecessors_and_producer_artifact`       | IDs collide, overflow bounds, or provenance/source encoding diverges.                   | Actual runs with thread `m:core:0`, 127-character machine ID, all interpreter events and fatal budget. Assert event continuity, bounds, distinct entities and predecessor links; optionally export four actual runs for root schema validation.                           | Passing Rust assertions; root records independent artifact/schema integration in T03-integration.md.                                                                                                 |
| `policy_contract_rejects_mismatch_and_invalid_selection`        | A policy can silently contradict configuration or corrupt ownership.                    | Mismatching policy rejects construction; wrong queue choice99 becomes global internalEngine and censors work.                                                                                                                                                             | Passing. Production policy modules remain deferred.                                                                                                                                                  |
| `t02_program_workload_fixture_parity`                           | Rust accepts/rejects differently from relevant shared fixtures or loses large integers. | The eight T02 program/workload fixtures match validity; MAX arrival/budget remain exact; immutable source JSON equals input.                                                                                                                                              | Passing after correcting the initial erroneous coverage-count assertion14 to the independently enumerated8; no fixture validity assertion changed. Command/reply cases remain outside decoder scope. |
| `integer_lexical_range_and_parameter_boundaries`                | Decimal transport or operand validation accepts lossy/invalid values.                   | Reject JSON number, leading zero, plus, negative unsigned, exponent-string, whitespace and overflow. Preserve0, 2^53+1, MAX; accept i64 range declarations and MAX override; reject nonpositive/overflow/nonstring/unknown overrides. Switch0 accepted, budget0 rejected. | Passing; parse directly to integer types, never float for transport values.                                                                                                                          |
| `compatibility_precedence_and_closed_shapes`                    | Unsupported versions are silently coerced or unknown fields accepted.                   | Simultaneous unsupported discriminators reject schema/model/engine/random in order; malformed nonversion shape takes precedence. Extra fields at six object levels, mixed operands and trailing JSON reject.                                                              | Passing; retain stable rejection semantics on fixes.                                                                                                                                                 |
| `resource_limits_exact_depth_blocks_bytes_and_counts`           | Resource bounds reject valid boundaries or admit oversized imports.                     | Depth64 passes/65 fails; flat10,000 blocks passes but recursively counted10,001 fails; raw1MiB passes/+1 byte fails;128 Unicode scalars pass/129 fail; cores1/64 pass,0/65 fail;129 parameters fails.                                                                     | Passing; resource-limit assertions remain explicit.                                                                                                                                                  |
| `resource_limit_preflight_precedes_unrelated_shape_errors`      | A malformed workload field can hide a resource breach found later in the document.     | Independently construct 129 valid parameter declarations plus authored machine ID `1invalid`; routing step 8 requires `ResourceLimit`. At exactly128 declarations with the same invalid ID, no limit is breached and ordinary validation requires `InvalidCommand`. | Passing; whole-input preflight returns the expected precedence and preserves the exact boundary. |

## Checks and residual scope

Commands use `C:\Users\Derek\.cargo\bin` prepended to PATH. Final command outcomes and deliberate negative-control results are appended after the bounded author rerun. The initial Clippy run found a test string comparison allocating inside a filter; computing the expected tick string once fixes the lint without changing the assertion. Earlier draft test failures were only the incorrect fixture subset count; they were retained as open until the corrected rerun.

No production FCFS/RR, metrics aggregation, retention policy, worker protocol, saved-run replay, performance benchmark, or simulation-native/Wasm equivalence is claimed. Pending independent reviewer findings must be corrected and rerun or explicitly block integration. Root owns full application and producer-schema checks. No agent or background helper is launched by this unit.

Final bounded author run: `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo test --workspace --locked`, and `git -c safe.directory=D:/codex/_projects/OS-laboratory/.verification/worktrees/t03 diff --check` all exited 0. Rust tests passed 17 sim-core tests plus the existing native CLI process integration test; empty adapter/doc suites are not additional simulation evidence. The resource family includes raw numeric cores `1.0`/`1e0` accepted and `1.5` rejected to match JSON Schema integer semantics; tick/duration inputs still require canonical strings.

Deliberate negative control: temporarily changed insertion preflight from `checked_add(count)` to `checked_add(0)`, leaving the real queue insertion checks active. `cargo test -p sim-core --locked insertion_preflight_rejects_before_operation_event` failed with exit 101 because the forbidden `dispatchStarted` event appeared. Restored the exact original source in a `finally` block; the same command then passed, and the complete final focused checks above passed afterward. This challenges operation atomicity rather than merely rejecting an invalid input. No mutation remains.

Author source is ready for independent review; no known failing author test remains. The root-owned handoff and integration record track the final commit, application checks and reviewer result. This unit leaves no tool process session or background activity running.

### D1 correction evidence

The null hypothesis is that an earlier ordinary shape error can hide a later resource-limit breach. The new `resource_limit_preflight_precedes_unrelated_shape_errors` case independently constructs 129 valid parameter declarations with authored machine ID `1invalid`; routing step 8 requires `ResourceLimit`. Its 128-declaration boundary companion requires ordinary `InvalidCommand`. Both assertions passed. `cargo test -p sim-core input::tests --locked` passed 5/5; `cargo fmt --all -- --check` and `cargo clippy -p sim-core --all-targets --locked -- -D warnings` also passed. The preflight covers workload program/thread counts, per-program parameters, thread overrides, recursively counted block limits, and repeat depth before ordinary field validation; failures remain actionable through the focused regression.

# T04 FCFS verification

Date: 2026-09-14. Branch: `codex/t04-fcfs`. Baseline: integrated T03 `1e1681d`. Candidate source began from interrupted checkpoint `a7b9206`.

Expected results below come from `sched.m1/revision-1` and the independently worked fixtures in `tests/reference/scheduling/README.md`, not from the production policy.

| Test or family | Null hypothesis (failure claim) | What is exercised | Expected result | Actual result and response |
|---|---|---|---|---|
| Policy configuration/selection | Production FCFS reports the wrong persisted policy or quantum, mutates the queue, or selects something other than its head. | `FcfsPolicy` directly with queue `[4,1,9]`. | `Fcfs`, no quantum, selection `4`, unchanged queue. | PASS in the focused suite. Any mismatch requires correction in `fcfs.rs`, not relaxation of the contract. |
| C01 and repeat determinism | Simultaneous arrivals do not remain declaration-ordered, or identical executions diverge. | Three FCFS threads with useful lengths 2/1/1; complete events, raw intervals, and terminal state compared across two executions. | Dispatch A/B/C; core useful `[0,2)`, `[2,3)`, `[3,4)`; final tick 4; both executions identical. | PASS. Correct production policy order and canonical repeat equality observed. |
| C03 dispatch accounting | Reservation leaves a thread ready during overhead or merges ready wait and dispatch overhead. | One core, switch cost 1, A at 0 and B at 1. | A overhead/useful `[0,1)`/`[1,3)`; B ready/overhead/useful `[1,3)`/`[3,4)`/`[4,5)`; final tick 5. | PASS. Raw core and B thread intervals matched exactly. |
| C05 idle/delayed arrival | The run omits pre-arrival idle time or dispatches the only thread to a higher core. | Two cores, switch cost 1, one arrival at tick 2. | Core 0 idle/overhead/useful `[0,2)`/`[2,3)`/`[3,5)`; core 1 idle `[0,5)`; dispatch on core 0. | PASS. Both core ledgers and dispatch event matched exactly. |
| C06 stable multicore dispatch | Simultaneous work is not assigned in stable queue/core order. | Three equal threads, two cores, switch cost 1. | A to core 0, B to core 1, then C to core 0; exact ledgers through final tick 6. | PASS. Dispatch sequence and both core ledgers matched exactly. |
| C07 explicit yield | FCFS retains a yielding thread or reinserts it ahead of an older waiter. | A computes/yields/computes while B waits. | Dispatch A/B/A; useful `[0,1)`, `[1,3)`, `[3,4)`; A ready `[1,3)`. | PASS. Dispatch order and raw core/thread intervals matched exactly. |
| C08 phase-4 fixpoint | A zero-cost immediate end strands ready work until an invented later tick. | A=`end`, B=`compute 1; end`, one core. | A terminates at tick 0, B dispatches in the same tick and runs `[0,1)`; final tick 1. | PASS. Correct `terminated` contract kind, same-tick dispatches, and absence of fabricated A intervals observed. |
| C10 no-compute I/O | Blocked work is selected, a useful interval is invented, or completion cannot occur without compute. | A=`io_wait 1; end`, one core. | A blocked and core idle `[0,1)`; redispatch/termination at tick 1; no running interval for A. | PASS. Raw blocked/idle intervals and termination tick matched exactly. |
| Persisted-policy mismatch | An FCFS instance silently accepts round-robin input. | Decode a valid RR workload and construct `Kernel<FcfsPolicy>`. | Constructor rejects the mismatch. | PASS. Existing T03 guard rejected it. |

## Commands and negative control

- After repairing the interrupted assertion and completing reference coverage, `cargo test -p sim-core fcfs --locked` passed 9/9 tests.
- Negative control: temporarily changed `FcfsPolicy::select` from `ready.front()` to `ready.back()`. `cargo test -p sim-core fcfs::tests::policy_selects_fifo_head_without_mutating_queue --locked` failed as required, exit 1: actual `Some(9)`, expected `Some(4)`. The mutation was restored.
- After restoration, `cargo test -p sim-core fcfs --locked` again passed 9/9 tests.

- Final author gate: `cargo fmt --all -- --check` passed; `cargo clippy -p sim-core --all-targets --locked -- -D warnings` passed; `cargo test -p sim-core fcfs --locked` passed 9/9; and `git diff --check` passed.

Independent T04 review and protected integration are separate gates and are not claimed here. No browser/full build is required by the T04 packet.

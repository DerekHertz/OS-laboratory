# T04 interrupted verification

Date: 2026-09-14. Branch: `codex/t04-fcfs`. Baseline: integrated T03 `1e1681d`. Status: **INCOMPLETE / FAILING**.

| Field | Evidence |
|---|---|
| Test | `cargo test -p sim-core fcfs --locked` |
| Null hypothesis | The interrupted FCFS draft satisfies its four current focused tests. |
| Testing for | Production policy configuration/FIFO choice, deterministic real-kernel execution, dispatch/yield interval behavior, and rejection of an RR workload. |
| Expected | Four tests pass before author checkpoint or review. The termination event kind must match the accepted T03 transport contract (`terminated`). |
| Actual | **FAIL**, exit 101: 3 passed, 1 failed. `dispatch_cost_yield_tail_and_immediate_end_use_kernel_phase_closure` asserts nonexistent kind `threadTerminated` at `fcfs_tests.rs:131`; the T03 kernel emits `terminated`. |
| Issue response | Next session should correct the expected event kind, verify the rest of the fixture rather than weakening it, add the missing applicable C05/C06/C08/C10 coverage required by the task packet, complete the decision/evidence docs, then rerun fmt/Clippy/focused tests and independent review. |

No negative control, Rustfmt, Clippy, full workspace check, or independent T04 review was completed. Three passing tests are bounded partial evidence and do not establish T04 acceptance. No source was changed after the failing run.
